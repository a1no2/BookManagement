<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kenji01.bookmanagement">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <user-sdk android:minSdkVersion="14" />

        <activity android:name=".PossessionListActivity" />
        <activity android:name=".WishListActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".RakutenSearch" />
        <activity android:name=".resultActivity" />
        <activity android:name=".StatisticsActivity" />
        <activity android:name=".seriesListActivity"></activity>
    </application>

</manifest>